server:
  port: 80

spring:
  application:
    name: studytime
  datasource:                           # docker-compose? ???? DB ?? ?????.
    url: jdbc:mysql://127.0.0.1:3306/studytime?zeroDateTimeBehavior=convertToNull&characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: jiwon
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:                             # Spring Boot2?? default DBCP? hikariCP???.
      maximum-pool-size: 20             # pool? ?? ??? ?? connection ?
      # minimum-idle: 20                  maximum-pool-size? ????? ??? ?????.
#  batch:
#    job:
#      enabled: false # ??? true? Spring Boot ?? ? ???? ?? Job?? ?????.
#    jdbc:
#      # Batch? ???? SQL ??? ????? ?????. ????? ???? ????? ?????, always ??? ?? ???? ???? ?? ????? ? ????.
#      # ?? ??? ???? ???? ???.
#      initialize-schema: always

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        #show_sql: true
        format_sql: true
        use_sql_comments: true
        default_batch_fetch_size: 100
    #open-in-view: false
logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace
